{
  "name": "@platform/observability",
  "version": "0.1.0",
  "private": true,
  "description": "Structured logging, correlation context and privacy-preserving log helpers.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "pino": "catalog:"
  },
  "devDependencies": {
    "eslint": "catalog:",
    "pino-pretty": "13.1.2",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
