{
  "name": "@platform/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Business rules for notification delivery. Depends on no infrastructure.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
