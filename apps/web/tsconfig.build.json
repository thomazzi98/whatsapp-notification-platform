{
  "extends": "./tsconfig.json",
  // The production build compiles what ships. Tests are typechecked by the
  // typecheck task, which includes them along with the testing helpers.
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["src/**/*.test.ts", "src/**/*.test.tsx"]
}
